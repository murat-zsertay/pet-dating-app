// import express from "express";
// import { PostsController } from "../controllers/posts.js";
//
// export const postsRouter = express.Router();
//
// postsRouter.get("/:post_id", PostsController.FindPostById);
// postsRouter.get("/", PostsController.Index);
// postsRouter.post("/", PostsController.Create);
// postsRouter.post("/:post_id/comment", PostsController.CreateComment);
// // Added an extra route here in order to handle the upload of the photo
// postsRouter.post("/", PostsController.FormHandler, PostsController.Create);
